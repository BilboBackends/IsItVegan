# Secrets â€” never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Database (local MVP data)
*.db
*.sqlite
*.sqlite3

# Local chat-classification exchange files (contain full scraped menus)
classification_jobs/

# Local working notes / session logs — not for the public repo
docs/

# Supabase CLI scratch state (project internals; regenerated on link)
supabase/.temp/

# Wrangler CLI scratch state
.wrangler/

# Raw dish snapshot: 60+ MiB rewritten every publish (history bloat) and
# over Cloudflare Pages' 25 MiB asset cap. The site serves dishes.json.gz
# plus per-restaurant shards; publish_static.py still writes this file
# locally (export_restaurant patches it in place).
frontend/public/data/dishes.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
