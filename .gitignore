# Secrets â€” never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Database (local MVP data)
*.db
*.sqlite
*.sqlite3

# Local chat-classification exchange files (contain full scraped menus)
classification_jobs/

# Local working notes / session logs — not for the public repo
docs/

# Supabase CLI scratch state (project internals; regenerated on link)
supabase/.temp/

# Wrangler CLI scratch state
.wrangler/

# Raw dish snapshot: still over Cloudflare Pages' 25 MiB asset cap and
# rewritten every publish. The site serves compact dishes-v2.json.gz plus
# full per-restaurant shards; this plain copy stays local for targeted edits.
frontend/public/data/dishes.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
